name: Swift
on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
jobs:
  build:
    runs-on: macos-26
    continue-on-error: false
    steps:
    - uses: actions/checkout@v4
    - name: Build
      run: swift build -v
